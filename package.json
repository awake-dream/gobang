{
  "name": "gobang",
  "version": "1.0.0",
  "description": "A simple Gomoku (Five in a Row) game",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "npx http-server . -p 8080 -c-1",
    "build": "echo 'No build required for vanilla JS'"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
